# v0.3.2

## 新增

- “管理备份”列出每个升级批次的时间、文件数、占用空间和当前回滚引用状态。
- 用户可选择单个批次，在确认后永久删除；升级进行中或目录含非备份文件时拒绝删除。
- 删除后清除指向该批次的回滚记录，其他批次和实例数据保持不变。

## 验证

- 隔离目录测试覆盖选择性删除、路径越界、符号链接和升级锁冲突。
- 图形界面验证备份列表与删除确认；取消后原备份保留。
- Apple Silicon 构建、签名、公证与 Gatekeeper 检查通过。

备份不会自动删除。永久删除无法撤销，请先确认升级后的实例登录与项目正常。Google Antigravity 程序不包含在安装包中。
