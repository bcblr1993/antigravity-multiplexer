# v0.3.3

## 新增

- 每个 Antigravity 副本可在“…”菜单中选择“销毁实例”。确认页列出将清除的位置，并要求输入完整实例名称。
- 销毁只处理选中的副本：应用、独立账号资料、窗口数据、日志、独立登录文件和管理清单；已有升级备份保留。

## 保护

- 主实例不可销毁；实例仍在运行、身份或路径不匹配、文件被其他实例引用、创建或升级正在进行时拒绝操作。
- 销毁前先将目标移入专属暂存目录；转移失败会回滚，避免只移走部分数据。

## 验证

- 隔离目录测试覆盖旧副本、新副本、主实例保护、运行进程、符号链接、清单异常、共享凭据和操作锁。
- Apple Silicon 构建与图形界面确认流程验证通过。

永久销毁无法撤销。已有升级备份可在管理器中另行手动删除。Google Antigravity 程序不包含在安装包中。
