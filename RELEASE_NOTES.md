# v0.3.5

修复副本已经退出，列表仍显示“运行中”的问题。

- 监听应用启动、退出和管理器重新激活，自动更新实例运行状态。
- 每两秒补充检查一次，避免状态停留在首次加载时；仅更新运行状态，不重复扫描备份。
- 同时匹配应用标识与实际应用路径，并排除已退出进程，避免同标识的其他副本造成误报。

保留 Antigravity 2.17.0 适配和从本机主实例升级功能。本次更新不替换 Antigravity 副本，不修改账号数据。
